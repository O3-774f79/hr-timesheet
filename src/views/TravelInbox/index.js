import React,{ PureComponent } from "react";
import DataTable from './FormReq'
export default class index extends PureComponent {
     render(){
         return(
            <DataTable />
         )
     }
}